/*
TCSS 360
Spirng 2024
 */
package Tests;
import static org.junit.Assert.*;
import org.junit.Before;
import org.junit.Test;
import Model.Door;

/**
 * Test cases for door class.
 */

/*
public class DoorTest {

    private Door door;

    @Before
    public void setUp() {
        door = new Door();
    }

    @Test
    public void testInitialDoorStatus() {
        assertEquals(Door.CLOSED, door.getDoorStatus());
    }

    @Test
    public void testUnlock() {
        door.unlock();
        assertEquals(Door.OPEN, door.getDoorStatus());
    }

    @Test
    public void testGetDoorIsLockedInitial() {
        assertFalse(door.getDoorIsLocked());
    }

    @Test
    public void testGetDoorIsLocked() {
        door.setDoorStatus(Door.LOCKED);
        assertTrue(door.getDoorIsLocked());
    }

    @Test
    public void testSetDoorStatusToOpen() {
        door.setDoorStatus(Door.OPEN);
        assertEquals(Door.OPEN, door.getDoorStatus());
    }

    @Test
    public void testSetDoorStatusToClosed() {
        door.setDoorStatus(Door.CLOSED);
        assertEquals(Door.CLOSED, door.getDoorStatus());
    }

    @Test
    public void testSetDoorStatusToLocked() {
        door.setDoorStatus(Door.LOCKED);
        assertEquals(Door.LOCKED, door.getDoorStatus());
    }
}


 */